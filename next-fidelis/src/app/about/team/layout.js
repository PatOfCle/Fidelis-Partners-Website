

export const metadata = {
    title: 'The Team',
    description: 'Known for being on the front lines, manufacturers have been elated for decades by the unmatched level of commitment capability brought by Fidelis employees.',
}

export default function layout({ children }) {
  return (
    <div>
      {children}
    </div>
  )
}
